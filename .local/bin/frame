cleanup() {
  tput cnorm >/dev/null 2>&1 || true
}

trap cleanup INT TERM EXIT

tput civis

if [ -t 0 ] && [ "$#" -eq 0 ]; then
  printf 'Usage: frame [file ...]\n' >&2
  exit 1
fi

if [ "$#" -gt 0 ]; then
  cat "$@"
else
  cat
fi

sleep infinity
